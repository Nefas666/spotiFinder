/**NOTE:
 * In order to run the app you'll be required to edit your next.config.js file as follows.
 * Remember to insert your personal keys and remove the next.config.js from the .gitignore before building to production
 */

/** @type {import('next').NextConfig} */
/** const nextConfig = {
  experimental: {
    appDir: true,
    serverComponentsExternalPackages: ["@tremor/react"],
  },
};

module.exports = {
  nextConfig,
  env: {
    SPOTIFY_CLIENT_ID: "your_client_id",
    SPOTIFY_CLIENT_SECRET: "your_secret_key",
    SPOTIFY_CALLBACK_ENDPOINT_LOCAL: "local endpoint uri",
    SPOTIFY_CALLBACK_ENDPOINT_PROD: "prod endpoint uri",
  },
};*/
